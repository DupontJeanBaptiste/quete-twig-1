<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bonjour</title>
    <link rel="icon" href="/assets/images/favicon.png">
    <link rel="stylesheet" href="/assets/css/styles.css">
</head>

<body>
    <h1>Hello this is a liste of products :</h1>
    <p>
    {% for product in tableau %}
        {% if product|length == 5 %}
            {{ product }} <br>
        {% else %}
            {{ product }}
        {% endif %}
    {% endfor %}
    </p>
    <p>
    {{ dump(tableau) }}
    </p>
</body>
</html>